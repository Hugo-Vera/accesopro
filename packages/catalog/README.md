# @accesopro/catalog

Catálogo de módulos tildables. Fuente de verdad para nombres, dependencias y resúmenes.

Usado por `apps/api` (habilitar módulos por tenant) y `apps/web` (sidebar, placeholders).

```ts
import { MODULE_CATALOG, moduleByKey, isModuleKey } from "@accesopro/catalog";
```
